from setuptools import setup

install_requires = [
    # nothing
]

packages = [
    "logdotprint",
]

setup(
    name="logdotprint",
    version="0.0.0",
    packages=packages,
    install_requires=install_requires,
)