from setuptools import setup

setup(name='distribute_bino_gauss',
      version='0.1',
      author='hqv',
      description='distributions for gaussian and binomial',
      packages=['distribute_bino_gauss'],
      zip_safe=False)
